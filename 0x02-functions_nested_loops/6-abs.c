#include <stdio.h>
#include <stdlib.h>
#include "holberton.h"

/**
 * _abs - Prints the absolute value of a number
 * @n: The number to convert to absolute
 * Return: 0
 */

int _abs(int n)
{
	_putchar(abs(n));

	return (0);
}
