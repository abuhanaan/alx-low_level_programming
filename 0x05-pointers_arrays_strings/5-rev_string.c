#include "holberton.h"
#include <string.h>

/**
 * rev_string - Prints a tring in reverse
 * @s: the string in question
 * Return: 0
 */

void rev_string(char *s)
{
	int i, l;

	l = strlen(s);
	for (i = l - 1; i >= 0; i--)
	{
	}
}
